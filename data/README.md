# Dataset directory

Raw images and generated train/validation/test splits are intentionally excluded from Git.

Prepare the supplied archive with:

```bash
python -m src.prepare_data --archive "D:\GitHub Projects\smart-waste-classification-edge-ai\data\dataset.zip"
```
